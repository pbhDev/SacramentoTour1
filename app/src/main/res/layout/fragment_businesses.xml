<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/fragment_layout_businesses"
    tools:context=".Businesses">

    <LinearLayout
        android:id="@+id/layout_businesses"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="@dimen/mid10dp"
        android:orientation="vertical">

    <ImageView
        android:id="@+id/imageView_business"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.4"
        android:layout_marginBottom="@dimen/mid10dp"
        android:elevation="@dimen/mid10dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher_background"
        android:contentDescription="@string/image_of_a_business" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerView_businesses"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.6"/>
    </LinearLayout>

</FrameLayout>